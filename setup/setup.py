# todo Create venv, install requirements, install mysql on host if doesn't exist
# todo maybe create a user who will rule them all











# For SQL
# mysql> CREATE USER 'scripts'@'localhost' IDENTIFIED BY 'PASSWORD';
# mysql> CREATE DATABASE skills;
# mysql> GRANT ALL PRIVILEGES ON skills.* TO 'scripts'@'localhost';
# mysql> FLUSH PRIVILEGES;
# todo then put these settings in local .env file!

